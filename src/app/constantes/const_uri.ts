import { environment } from "src/environments/environment";

export const const_uri = {
   
    mant_paquete: `${environment.uri_back_end}paquete/`,
    mant_reserva: `${environment.uri_back_end}reserva/`,
    mant_pago: `${environment.uri_back_end}pago/`,
    mant_cliente: `${environment.uri_back_end}cliente/`,
    mant_proveedor: `${environment.uri_back_end}proveedor/`,
    mant_destino: `${environment.uri_back_end}destino/`,
    mant_usuario: `${environment.uri_back_end}usuario/`,
    mant_rol: `${environment.uri_back_end}role/`
}
