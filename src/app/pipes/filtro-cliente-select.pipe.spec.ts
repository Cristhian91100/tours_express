import { FiltroClienteSelectPipe } from './filtro-cliente-select.pipe';

describe('FiltroClienteSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroClienteSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
