import { FiltroDestinoPipe } from './filtro-destino.pipe';

describe('FiltroDestinoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDestinoPipe();
    expect(pipe).toBeTruthy();
  });
});
