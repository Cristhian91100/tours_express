import { FiltroPaqueteSelectPipe } from './filtro-paquete-select.pipe';

describe('FiltroPaqueteSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPaqueteSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
