import { FiltroPaquetesPipe } from './filtro-paquete.pipe';

describe('FiltroPaquetesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPaquetesPipe();
    expect(pipe).toBeTruthy();
  });
});
